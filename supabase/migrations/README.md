# FALAH Supabase migrations

These files are the repository source of truth for the FALAH attendance schema. The live project was bootstrapped interactively before this repository was fully synchronized, so some repository migration files consolidate equivalent live changes. New schema changes should be added as forward-only migrations.
